# from app.database.database import Base,engine,session
# from app.models.models import Markers
# print(session.query(Markers.custom_name, Markers.lat, Markers.lon).all())
from dotenv import load_dotenv
load_dotenv()